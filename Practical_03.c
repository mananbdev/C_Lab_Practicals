/* 3.	Write a program to add two numbers and display the sum.	*/

#include <stdio.h>
int main()
{
    int num1 , num2;
    printf("Enter two numbers : ");
    scanf("%d",&num1);
    scanf("%d",&num2);
    printf("Sum of two numbers : %d",(num1+num2));
    return 0;
     
}